#include "main.h"

/**
 * swap_int - function that swaps the values of two integers
 * @a: swaping with b
 * @b: swaping with a
 *
 * Return: always 0 .
 */
void swap_int(int *a, int *b)
{
	int bat

		bat = *a;
		*a = *b;
		*b = bat;
}
